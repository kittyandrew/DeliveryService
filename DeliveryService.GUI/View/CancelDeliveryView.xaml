<UserControl x:Class="DeliveryService.GUI.View.CancelDeliveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeliveryService.GUI.View"
             xmlns:viewmodel="clr-namespace:DeliveryService.GUI.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:CancelDeliveryViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
        <TextBlock
            FontSize="15" FontWeight="Bold"
            Text="Choose your delivery from the list below and click the button to remove."
        />
        <Button
                Margin="25"
                Height="60" Width="100"
                Command="{Binding CancelDeliveryCommand}" 
                Content="Cancel delivery"
            />
    </StackPanel>
</UserControl>
